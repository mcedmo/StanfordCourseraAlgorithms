#include <math.h>

main()
{
    kar(2000, 2000);
}

int kar(int x, int y){
    int a, b, c, d, n;

    n = strlen(n);
    a = 



    /* base case */
    if(x < 10 and y < 10) {
        return x * y;
    }
    else {
        return ((pow(10, n) * kar(a, c)) + (pow(10, n/2) * (kar(a, d) * kar(b, c))) + kar(b, d));
    }
}
